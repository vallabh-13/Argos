"""Placeholder test for Phase 0.

This file exists only so the CI test runner has something to collect and pass,
keeping the build green from commit one. It will be replaced by real SDK tests
in Phase 1 (SDK + a span).
"""


def test_placeholder():
    """Trivially passes — proves the test pipeline runs end to end."""
    assert True
